# Changelog

## 1.0.2

- 初版リリース。

## 1.0.3

- `Apply Destructively` 実行時、 `AnimatorController` と `AnimationClip` にプレフィックスを付与して複製するように変更。

## 1.1.0

- `Apply Destructively` で複製後コンポーネントのリストを返却するように変更。

## 1.1.1

- BrendTree の複製が正しく行われない問題を修正。
